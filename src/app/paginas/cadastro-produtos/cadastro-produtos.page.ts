import { Component, OnInit } from '@angular/core';
import { Validator, FormBuilder, ReactiveFormsModule, FormsModule} from '@angular/forms';
import { Http, Headers, Response, ResponseOptions} from '@angnpm i @angular/httpular/http';

@Component({
  selector: 'app-cadastro-produtos',
  templateUrl: './cadastro-produtos.page.html',
  styleUrls: ['./cadastro-produtos.page.scss'],
})
export class CadastroProdutosPage implements OnInit {

  constructor() { }

  ngOnInit() {
  }

}
